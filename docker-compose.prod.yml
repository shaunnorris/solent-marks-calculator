version: '3.8'

# Production environment - SSL with Let's Encrypt
services:
  web:
    ports:
      - "127.0.0.1:8000:8000"
    restart: always
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
